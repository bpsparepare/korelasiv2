<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </base-config>
    <!-- Opsional: Jika Anda ingin mengizinkan domain tertentu tanpa validasi sertifikat penuh,
         ini TIDAK DISARANKAN untuk produksi. Hanya untuk debugging jika sangat diperlukan. -->
    <!-- <domain-config>
        <domain includeSubdomains="true">script.google.com</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config> -->
</network-security-config>
